[package]
name = "strategic_patch"
version = "0.1.0"
edition = "2024"

[dependencies]
indexmap = "2.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
strategic_patch_derive = { path = "../strategic_patch_derive", optional = true }
thiserror = "1.0"

[features]
default = []
derive = ["dep:strategic_patch_derive"]
